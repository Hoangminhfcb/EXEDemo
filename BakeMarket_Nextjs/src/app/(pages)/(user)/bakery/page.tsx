import { LoadingSpinner } from "@/components/ui/LoadingSpinner";

export default function BakerysPage() {
  return (
    <div className="flex flex-col items-center justify-center h-screen">
      <LoadingSpinner />
    </div>
  );
}
